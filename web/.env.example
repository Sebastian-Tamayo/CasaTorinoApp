# web / TPV — Vercel Environment Variables (nunca subir valores reales)

TPV_PIN=
TPV_SYNC_KEY=
# Opcional: protege el cron /api/ops-daily-purge (si no, se usa TPV_SYNC_KEY)
CRON_SECRET=

# Persistencia operativa (ops_kv)
SUPABASE_URL=
SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=

# Legacy Edge Config (ya no usados por cocina/tpv actuales)
TPV_EDGE_CONFIG_ID=
TPV_TEAM_ID=
TPV_VERCEL_TOKEN=
